# SDD Execution — Wave E0 + R0 (path to 100%)

**Date:** 2026-07-25  
**Parent:** [path-to-100-percent](./2026-07-25-path-to-100-percent.md) · [remaining-completion-priority](./2026-07-25-remaining-completion-priority.md)  
**Method:** Subagent-Driven — one fresh subagent per task, review between tasks, continue until Gate or owner BLOCKED.

## Global Constraints

- Do **not** claim CPC thương mại 100% or E100.
- Do **not** invent Meta credentials or spend Render/Supabase money.
- Owner blockers (payment, Meta dashboard) → document **BLOCKED** with exact next action; that is an acceptable task exit.
- Prefer worktree branch `feat/sdd-e0-r0-completion`; commits on feature branch only.
- No secrets in git; `.local-secrets/` only.
- Keep diffs minimal and on-task.
- Warehouse fix commit `f4ba460` (or equivalent) must be on the branch before Task 3 walkthrough re-verify.
- Prior Wave R0 SDD (`feat/sdd-r0-completion`) docs may be merged in — do not re-do R0.2/R0.4 prep if already present; verify and extend.

## Already DONE (skip)

- R0.1 migrations CI + remote staging (26 migrations; new warehouse migration may still need staging push)
- R0.5 Scheduled QA
- Prior SDD R0 Tasks 1–4 docs on `feat/sdd-r0-completion` (walkthrough partial, R0.2 unblock pack, R0.4 prep, gate summary)
- Local warehouse migration applied via `db reset` on fix branch (local only)

---

## Task 1: Branch integrate — warehouse fix + R0 SDD docs + path-to-100

**Goal:** One working branch with E0.1 code + prior R0 SDD docs + path-to-100 plan.

### Steps

1. Ensure branch `feat/sdd-e0-r0-completion` contains:
   - `fix(orders): ensure default warehouse…` (migration + API map + tests)
   - Docs from `feat/sdd-r0-completion` if not already present (walkthrough, evidence, meta prep, deploy always-on section, SDD R0 plan)
   - `docs/superpowers/plans/2026-07-25-path-to-100-percent.md`
   - `docs/superpowers/plans/2026-07-25-sdd-e0-r0.md` (this file)
2. Resolve merge conflicts favoring newer accurate statuses.
3. Commit if needed: `chore(sdd): integrate E0 warehouse fix + R0 docs + path-to-100`.

### Done when

- Single branch tip has warehouse migration file + path-to-100 + R0 evidence docs.
- `pnpm --filter api test -- orders.service.spec.ts` still passes.

---

## Task 2: E0.2 — Local Inngest / knowledge_chunks verify

**Goal:** Prove or honestly BLOCK knowledge reindex path locally.

### Steps

1. Ensure local Supabase + API (+ AI) up per `docs/ops/local-host.md`.
2. Attempt Inngest dev (`npx inngest-cli@latest dev -u http://127.0.0.1:3001/api/inngest`) if feasible in this environment; otherwise document BLOCKED reason.
3. Create a product (RPC or API); check `outbox_events` for `knowledge.reindex`; check `knowledge_chunks` count.
4. Update walkthrough criterion 3 and/or `local-host.md` with result PASS / BLOCKED.
5. Commit: `docs(ops): E0.2 knowledge reindex local verify (SDD)`.

### Done when

- Evidence recorded (PASS with chunks>0 **or** BLOCKED with exact command owner/eng must run).
- No fake PASS.

---

## Task 3: E0.3 — Re-verify §12.1 local confirm after warehouse fix

**Goal:** Criterion 5 must no longer be FAIL due to missing warehouse.

### Steps

1. With local DB that has migration `20260727210000_ensure_default_warehouse_on_org.sql` applied.
2. Create org → verify default warehouse exists.
3. Create product with `stock_qty >= 1` → draft order → confirm → expect `confirmed` (RPC or API).
4. Update `docs/ops/p0-staging-walkthrough-12-1.md` criterion 5 to PASS (or PASS partial if export-only gap).
5. Update `docs/ops/r0-r3-execution-evidence.md` R0.3 row accordingly (still AMBER overall if Meta blocked).
6. Commit: `docs(ops): E0.3 §12.1 confirm PASS after warehouse fix (SDD)`.

### Done when

- Confirm proven GREEN locally; walkthrough criterion 5 updated; no claim R0.3 overall GREEN.

---

## Task 4: E0.4 — CPC stub decision table (owner blanks)

**Goal:** Add decision rows for Zalo / e-invoice / advisor so R2 later knows what is required.

### Steps

1. Update `docs/superpowers/plans/cpc-checklist.md` with a short **Stub decisions (owner)** table: R2.4 Zalo, R2.5 E-invoice, R2.6 Advisor — each `REQUIRED | AMBER_OK | undecided` with date blank for owner.
2. Do **not** invent owner choices; leave `undecided` unless already documented elsewhere.
3. Commit: `docs(cpc): E0.4 stub decision table for R2 (SDD)`.

### Done when

- Table present; no false REQUIRED/AMBER_OK without owner.

---

## Task 5: R0 staging — push warehouse migration if credentials allow

**Goal:** Staging schema includes `20260727210000_…` or document BLOCKED.

### Steps

1. Check `.local-secrets/` or env for `SUPABASE_ACCESS_TOKEN` / linked staging `tjsmpcgkeoglemptuymu`.
2. If available: `supabase db push` (or project-linked push) for staging; verify migration listed.
3. If not: update evidence R0.1 note “warehouse migration pending staging push” + exact command; status AMBER not GREEN for that delta.
4. Commit docs/evidence only (never commit tokens): `docs(ops): R0 staging warehouse migration status (SDD)`.

### Done when

- Staging has migration **or** BLOCKED with owner command list.

---

## Task 6: R0.2 / R0.4 owner gate pack — refresh + stop

**Goal:** Confirm owner unblock packs are current; do not fake GREEN.

### Steps

1. Verify `deploy-staging-render.md` always-on section and evidence R0.2 still say keep-warm ≠ GREEN.
2. Verify Meta prep pack URLs still match staging.
3. Append **E0+R0 SDD gate** section to `docs/ops/r0-r3-execution-evidence.md`:
   - E0.1–E0.4 statuses
   - R0 remaining: R0.2 payment, R0.4 META_*, R0.3b staging after Meta
   - Next wave = R1 only after Gate R0 (owner)
4. Update path-to-100 “tiếp theo ngay” if needed.
5. Commit: `docs(ops): E0+R0 SDD gate summary — owner blockers (SDD)`.

### Done when

- Gate summary accurate; controller will STOP after this task for owner (R0.2/R0.4).
- Do not start R1 paid billing in this wave.

---

## After this wave (controller)

**STOP for owner:** Render Starter payment (R0.2) + Meta credentials/submit (R0.4).  
Then human/agent resumes R0.3b staging full walkthrough → Gate R0 → Wave R1 SDD plan.
