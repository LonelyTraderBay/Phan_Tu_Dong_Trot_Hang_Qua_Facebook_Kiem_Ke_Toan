# SDD Execution — Wave E2 → path to 100%

**Date:** 2026-07-25  
**Parent:** [path-to-100-percent](./2026-07-25-path-to-100-percent.md)  
**Branch:** `cursor/e2-completion` (from `main` @ #20)  
**Method:** Subagent-Driven — one task per subagent, review between tasks, continue until Gate or owner/vendor BLOCKED.

## Global Constraints

- Do **not** claim CPC thương mại 100% or E100 until SoT gates are GREEN.
- Do **not** invent Meta credentials, Render payment, Supabase Pro billing, or SOC2 vendor contracts.
- Owner/vendor blockers → document BLOCKED with exact next action; acceptable exit.
- Prefer `cursor/` branch prefix; no secrets in git.
- E1 (resume/advisor/Zalo) must land on `main` before further eng (Task 1).
- Keep diffs on-task.

## Already on main (skip)

- E0 warehouse fix + R0 docs (#20)
- Pilot A–H scaffolding

## Already done off-main (must land)

- E1: inbox resume, Gemini advisor, Zalo persist (#21 merged only into `cursor/sdd-e0-r0-completion`)

---

## Task 1: Land E1 onto main

**Goal:** Resolve merge of `cursor/e1-eng-completion` into `cursor/e2-completion` (based on `main`), fix walkthrough conflict, commit, push, open PR to `main`.

### Steps

1. Resolve `docs/ops/p0-staging-walkthrough-12-1.md` conflict: keep E0.3 confirm PASS + E1 resume PASS statuses; Meta still BLOCKED.
2. Finish merge commit.
3. Run focused tests: inbox.service.spec, zalo-persist-inbound.spec, advise pytest (or note worktree vitest caveat and run via parent vitest).
4. Push `cursor/e2-completion` and create draft PR → `main`.
5. Do not wait for merge to continue Task 2+ on this branch.

### Done when

- Conflict resolved; PR open (or ready); E1 files present on branch tip.

---

## Task 2: Staging — push resume migration `20260727220000`

**Goal:** Staging has resume RPC or document BLOCKED.

### Steps

1. Use `.local-secrets/supabase-staging.json` (parent repo) for db push like Task 5 E0+R0.
2. Verify migration list includes `20260727220000`.
3. Update `docs/ops/r0-r3-execution-evidence.md` / migrate log.
4. Commit docs: `docs(ops): staging resume migration status (SDD E2)`.

### Done when

- Staging GREEN for resume migration **or** BLOCKED with commands.

---

## Task 3: R2.5 eng — e-invoice HTTP sandbox provider (beyond stub-only)

**Goal:** Add a second provider code (e.g. `http_sandbox`) that posts to configurable URL; keep `stub` default.

### Steps

1. Extend `EinvoiceProviderSchema` with `http_sandbox` (or similar).
2. Implement provider reading env/org config base URL + API key; mockable fetch; map response to existing job status.
3. Tests: stub still works; http_sandbox success + failure mapping.
4. Update OpenAPI/docs briefly if needed.
5. Commit: `feat(einvoice): http_sandbox provider for R2.5 eng path`.

### Done when

- Tests pass; stub remains default; no fake live tax compliance claim.

---

## Task 4: R1 eng prep — billing gate proof harness (no paid billing)

**Goal:** Automated test or script proving entitlement gates fire (`max_pages` / `auto_confirm`) without requiring Supabase Pro.

### Steps

1. Find existing entitlements/billing tests.
2. Add or extend a focused test that exceeds a limit and expects gate/403/blocked behavior.
3. Document in `plan-e-dod-evidence.md` what remains owner-paid (Pro/PITR/always-on) vs eng-proven.
4. Commit: `test(billing): entitlement gate proof for R1 eng prep`.

### Done when

- Test GREEN; evidence distinguishes eng vs owner R1 items.

---

## Task 5: Wave E2 gate + hard STOP for owner/vendor path to 100%

**Goal:** Accurate gate summary; next actions only owner/vendor can clear for CPC/E100 100%.

### Steps

1. Update `docs/ops/r0-r3-execution-evidence.md` with E2 section.
2. Update path-to-100 “tiếp theo ngay”: owner R0.2/R0.4 → R1 paid → R2.1–2.3 live → CPC → R3.
3. List eng % remaining vs live % remaining honestly.
4. Commit: `docs(ops): E2 SDD gate — eng landed; owner path to 100%`.
5. Controller STOPS after this task (cannot reach 100% without owner payment/Meta/vendors).

### Done when

- Gate doc accurate; no false 100% claim.

---

## After E2 (controller)

**STOP.** Remaining to 100% is owner/live/vendor:
- R0.2 Render Starter, R0.4 Meta App Review
- R1.0–R1.6 paid ops
- R2.1–R2.3 carrier/COD/returns live
- R3 SOC2/pen-test/SSO/SLA

Do not invent credentials. Resume SDD when owner unblocks or provides keys.
