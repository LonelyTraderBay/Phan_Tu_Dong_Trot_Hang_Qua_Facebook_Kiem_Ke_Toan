# SDD Execution — Wave L3 (Gate A — Code local READY)

**Date:** 2026-07-26  
**Parent:** [completion-priority-code-first](./2026-07-26-completion-priority-code-first.md) · [path-to-100](./2026-07-25-path-to-100-percent.md) · [completion-step-by-step](./2026-07-25-completion-step-by-step.md) · [remaining-priority](./2026-07-25-remaining-completion-priority.md) · [L2 plan](./2026-07-26-sdd-l2-code-complete.md)  
**Branch:** `cursor/l3-gate-a` (from `main` @ `e2105a6` after PR #27)  
**Worktree:** `.worktrees/l3-code`  
**Method:** Subagent-Driven — one task per subagent, review between tasks.

## Owner policy / SoT note (code-first Pha A)

Owner wants **Pha A code hoàn thiện trên PC** until **Gate A**. Commercial / Render / Meta / CPC claim is **deferred**.

- **NOW = Wave L3** — Pha A steps A1→A5 (+ Gate A evidence). Wave success = **Gate A Code local READY**.
- **“100%” for this wave** = Gate A local READY only — **not** CPC / E100 / tổng 100%.
- Do **not** claim CPC thương mại 100% or E100 100% in this wave.
- Render payment / Starter and live Meta App Review stay under Pha B (after Gate A + owner wants to sell).

## Global Constraints

- Local Docker / `dev:local` is the default coding + SDD surface.
- No Render payment required this wave.
- Meta walkthrough / live webhook rows may stay **BLOCKED** locally — OK.
- Gate A = wave success; commercial still deferred at STOP.
- Prefer `cursor/` branch prefix; no secrets in git (never print secret values).
- Keep diffs minimal and on-task.
- Wave L2 CLOSED remains true; L3 continues **eng local Pha A only**.

## Already DONE (skip)

- Wave L1: SoT local-first · stack verify · stub embeddings · E0.4 notes · gate (PR #26)
- Wave L2: invite list+accept · Inngest in `dev:local` · advisor aggregates · Zalo runbook · CI Node 22 · gate (PR #27)
- Regression baseline: API 185 · AI 37 · isolation 6 pass (skips may remain — A4)
- Local host playbook `docs/ops/local-host.md`

---

## Task 1: Land code-first SoT + this plan

**Goal:** Open L3 branch; land code-first checklist SoT + this plan; Wave L3 progress ledger; draft PR → `main`.

### Steps

1. Worktree `.worktrees/l3-code` on `cursor/l3-gate-a` from `origin/main`.
2. Apply uncommitted SoT: `2026-07-26-completion-priority-code-first.md` + path-to-100 / remaining / completion-step-by-step updates; clean parent `main`.
3. Write this plan; append Wave L3 progress ledger.
4. Commit: `docs(plan): code-first checklist + SDD L3 Gate A`.
5. Push; open **draft** PR → `main`.

### Done when

- Plan + code-first SoT + ledger on branch; parent `main` clean; no secrets; no CPC/E100 100% claim.

**Status:** DONE in this task.

---

## Task 2: A1 — Local walkthrough smoke post-L2

**Goal:** Prove non-Meta local walkthrough after L2 (invite accept, confirm, `knowledge_chunks` > 0).

### Steps

1. `pnpm run dev:local` (Docker Supabase + api/web/ai + Inngest).
2. Walkthrough non-Meta: health · org · invite create+accept · product · stock · draft→confirm · export · inbox resume · advisor suggest · stub reindex → `knowledge_chunks` > 0.
3. Update `docs/ops/p0-staging-walkthrough-12-1.md` (and evidence date) for non-Meta PASS; Meta may stay **BLOCKED**.
4. Commit when honest.

### Done when

- Non-Meta criteria PASS (invite accept + confirm + chunks > 0); Meta BLOCKED OK; walkthrough updated.

**Status:** PENDING.

---

## Task 3: A2 — Minimal local e2e smoke

**Goal:** One-command local e2e smoke (Playwright **or** API script) + `pnpm` script.

### Steps

1. Add Playwright flow **or** API script: signup/login → invite accept → catalog → confirm → export CSV (minimal path).
2. Wire `pnpm` script (e.g. `test:e2e:local`) documented for Docker stack.
3. Commit when green locally (CI optional if already covered by script docs).

### Done when

- One local command proves happy path; no secrets; no commercial claim.

**Status:** PENDING.

---

## Task 4: A3 — ESLint real OR remove unused + typecheck packages green

**Goal:** Honest lint story + monorepo typecheck green.

### Steps

1. Either land real ESLint flat config for api/web **or** remove unused `eslint` dep / empty scripts.
2. Ensure `lint` / `typecheck` cover `packages/*` as claimed.
3. `pnpm lint` + `pnpm typecheck` green (or document exact scope if partial — prefer full green).
4. Commit when green.

### Done when

- No fake lint; typecheck packages green; evidence in PR/CI log.

**Status:** PENDING.

---

## Task 5: A4 isolation skip fix + A5 OpenAPI/api-client honesty + Gate A evidence + STOP

**Goal:** Close A4/A5 enough for Gate A; append Gate A evidence; STOP with commercial still deferred.

### Steps

1. **A4:** Fix `tests/isolation` skips (implement with Docker Supabase) **or** replace with migration-level proof + remove permanent skips (ticket if temporary).
2. **A5:** Codegen from `packages/contracts/openapi.yaml` **or** mark `api-client` deprecated / honest README (no “codegen deferred” contradiction).
3. Tick Gate A checklist from [code-first § A8](./2026-07-26-completion-priority-code-first.md) honestly (A1–A5 + regressions).
4. Append Wave L3 Gate A section to `docs/ops/r0-r3-execution-evidence.md`.
5. Controller STOP: commercial (Render/Meta/CPC) still deferred — do **not** claim CPC/E100 100%.

### Done when

- Gate A evidence accurate; CPC/E100 still not 100%; no Render payment required for Done.

**Status:** PENDING.

---

## Self-check (every task)

- [ ] No secrets committed or printed
- [ ] No CPC / E100 / tổng 100% claim
- [ ] Wave “100%” means **Gate A Code local READY** only
- [ ] Render payment not required for task Done
- [ ] Meta BLOCKED locally is acceptable
- [ ] PowerShell: use `;` not `&&`
